<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
            android:id="@+id/back_to_main"
            android:layout_width="40dp"
            android:layout_marginLeft="16dp"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:layout_marginStart="16dp"
            android:src="@drawable/image_edit_back"
            tools:ignore="RtlHardcoded" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:orientation="horizontal">

        <LinearLayout
            android:id="@+id/brush_btn"
            style="@style/ImageEditorOptionStyle">

            <ImageView
                android:id="@+id/brush_icon"
                style="@style/ImageEditorIconStyle"
                android:layout_gravity="center"
                android:src="@drawable/ic_brush_white_24dp" />

            <TextView
                style="@style/ImageEditorOptionTextStyle"
                android:fontFamily="@font/poppins_regular"
                android:text="@string/image_editor_brush" />
        </LinearLayout>

        <LinearLayout
            android:id="@+id/eraser_btn"
            style="@style/ImageEditorOptionStyle">

            <ImageView
                android:id="@+id/eraser_icon"
                style="@style/ImageEditorIconStyle"
                android:layout_gravity="center"
                android:src="@drawable/ic_eraser_disabled" />

            <TextView
                style="@style/ImageEditorOptionTextStyle"
                android:fontFamily="@font/poppins_regular"
                android:text="@string/image_editor_eraser" />
        </LinearLayout>

    </LinearLayout>

    <ImageView
            android:id="@+id/settings"
            style="@style/ImageEditorSettingIconStyle"
            android:layout_alignParentTop="true"
            android:layout_alignParentRight="true"
            android:layout_alignParentEnd="true"
            android:src="@drawable/ic_settings_black_24dp"
            tools:ignore="RtlHardcoded" />

</RelativeLayout>
